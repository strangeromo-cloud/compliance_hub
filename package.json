{
  "name": "compliance-hub-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Bilingual Compliance Hub multi-agent prototype using public sources.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/generate-seed.mjs",
    "test": "node --test",
    "refresh-fallback": "node scripts/refresh-fallback.mjs",
    "check-model": "node scripts/check-model.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
